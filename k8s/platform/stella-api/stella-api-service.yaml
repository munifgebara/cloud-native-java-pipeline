apiVersion: v1
kind: Service
metadata:
  name: stella-api
  namespace: platform
spec:
  selector:
    app: stella-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP


